/*
 * Created by SharpDevelop.
 * User: Mauro
 * Date: 2013-05-14
 * Time: 10:47
 * 
 * To change this template use Tools | Options | Coding | Edit Standard Headers.
 */
using System;
using NUnit.Framework;

namespace SzondiTest
{
	[TestFixture]
	public class SerieFormTest
	{
		
		[Test]
		public void TestWriteFile()
		{/*
			Environment.GetFolderPath(Environment.SpecialFolder.CommonApplicationData);
			var decodedData 
				= new System.Collections.Generic.Dictionary<SzondiCounts.TestPhases, System.Collections.Generic.Dictionary<SzondiCounts.FactorsSelections, ushort>>(2);
			
			
			foreach(SzondiCounts.TestPhases testPhase
			        in (SzondiCounts.TestPhases[])Enum.GetValues(typeof(SzondiCounts.TestPhases)))
			{
				
				decodedData.Add(testPhase, new System.Collections.Generic.Dictionary<SzondiCounts.FactorsSelections, ushort>(16));
				
				foreach(var factorSelection 
				        in (SzondiCounts.FactorsSelections[])Enum.GetValues(typeof(SzondiCounts.FactorsSelections)))
				{
					decodedData[testPhase].Add(factorSelection, 2);
				}
				
			}
			SzondiCounts szondiCalculator = new SzondiCounts();
			szondiCalculator.decodedAndCountedSelections = decodedData;
			WriteReportToFile(szondiCalculator);
			*/
		}
	}
}
